add two new options to the tool.

1. if no filename is given as a parameter, search for a single .yml file in the current directory, and if just one exists use that.

2. support an optional parameter "--tree", and if given instead of running an interactive version of the menu, just dump it in a tree style structure to the terminal and then exit.

