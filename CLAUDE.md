# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This project has not been set up yet — the repository is currently empty (no source files,
README, package manifest, or configuration). There is nothing here yet to document in terms
of build/lint/test commands or architecture.

Once the project scaffolding exists (e.g. `package.json`, `Cargo.toml`, `pyproject.toml`, etc.),
re-run `/init` (or ask Claude to update this file) to populate it with:

- Commands to build, lint, test, and run the project (including running a single test)
- The high-level architecture and structure of the codebase
